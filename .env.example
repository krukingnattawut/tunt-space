# Copy this file to .env.local for local development.
# On Vercel, connect a database from the dashboard instead — it injects
# DATABASE_URL automatically and you won't need to set this manually.
DATABASE_URL=postgres://user:password@host/dbname

# Shared password teachers use to log into /teacher
TEACHER_PASSWORD=change-this-password
